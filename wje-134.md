<!-- 改编自：建筑仓储车辆主题项目提示词/wje-134/wje-134.md；后端由 NestJS + TypeScript 改造为 Go 1.22 + Gin + GORM -->

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 项目概述

【纯后端服务】建筑工程项目成本管控 API 服务。面向建筑公司的财务和项目管理层，提供项目预算编制、成本归集、变更管理和成本分析的 RESTful API。**本项目用于验证模型跨文件协同改动能力，禁止将多个职责合并进单一文件。**

### 核心实体（≥ 3 个，每个贯穿后端全链路）

1. **项目预算（ProjectBudget）** — 字段：所属项目ID、预算名称、总预算金额、已用金额、预留金额、币种（CNY/USD）、审批状态（Draft/Submitted/Approved/Rejected）、审批人ID、审批日期、备注
   - 关联：1个预算 → 多个成本项
   - 业务动作：编制预算、提交审批、审批通过/驳回
   - **贯穿后端**：DB表 → `backend/internal/model/project_budget.go` → `backend/internal/repository/project_budget_repository.go` → `backend/internal/service/project_budget_service.go` → `backend/internal/handler/project_budget_handler.go` → `backend/internal/router/budgets.go`

2. **成本项（CostItem）** — 字段：所属预算ID、成本类别（Material/Labor/Equipment/Subcontract/Overhead/Other）、成本名称、预算金额、实际金额、差异金额（自动计算）、发生日期、凭证号、关联材料领用ID（可选）、关联工时记录ID（可选）
   - 业务动作：录入成本、核对差异、标记异常
   - **贯穿后端**：DB表 → `backend/internal/model/cost_item.go` → `backend/internal/repository/cost_item_repository.go` → `backend/internal/service/cost_item_service.go` → `backend/internal/handler/cost_item_handler.go` → `backend/internal/router/cost_items.go`

3. **变更单（ChangeOrder）** — 字段：所属项目ID、变更类型（ScopeChange/DesignChange/PriceAdjustment/UnforeseenCondition）、变更描述、原预算金额、变更金额、变更后金额（自动计算）、申请理由、审批状态（Draft/Submitted/Approved/Rejected/Cancelled）、申请人ID、审批人ID、申请日期
   - 业务动作：发起变更、提交审批、审批/驳回、作废
   - **贯穿后端**：DB表 → `backend/internal/model/change_order.go` → `backend/internal/repository/change_order_repository.go` → `backend/internal/service/change_order_service.go` → `backend/internal/handler/change_order_handler.go` → `backend/internal/router/change_orders.go`

4. **成本分析报告（CostReport）** — 字段：所属项目ID、报告期间（月/季）、报告类型（Monthly/Quarterly/Annual/Custom）、人工成本汇总、材料成本汇总、设备成本汇总、其他成本汇总、总成本、盈亏分析、生成时间
   - **贯穿后端**：DB表 → `backend/internal/model/cost_report.go` → `backend/internal/repository/cost_report_repository.go` → `backend/internal/service/cost_report_service.go` → `backend/internal/handler/cost_report_handler.go` → `backend/internal/router/reports.go`

### 横切关注点（≥ 2 个）

1. **RBAC 权限控制**
   - 角色：Admin / FinanceManager / ProjectManager / Accountant / Viewer
   - 触达文件层：DB role表 + `backend/internal/middleware/rbac.go` + 各 handler 权限检查 + 类型定义 `backend/internal/constants/roles.go`

2. **审计日志**
   - 记录预算审批、成本录入、变更审批等关键操作
   - 触达文件层：DB audit_log表 + `backend/internal/middleware/audit_logger.go` + 各 service 埋点 + 类型定义 `backend/internal/model/audit_log.go`

3. **接口限流**
   - 触达文件层：`backend/internal/middleware/rate_limiter.go` + 配置文件 + Redis 计数器

### 共享枚举/常量（≥ 2 组）

1. **预算审批状态 BudgetStatus**：Draft / Submitted / Approved / Rejected
   - 定义：`backend/internal/constants/budget_status.go`

2. **成本类别 CostCategory**：Material / Labor / Equipment / Subcontract / Overhead / Other
   - 定义：`backend/internal/constants/cost_category.go`

3. **变更类型 ChangeType**：ScopeChange / DesignChange / PriceAdjustment / UnforeseenCondition
   - 定义：`backend/internal/constants/change_type.go`

4. **变更审批状态 ChangeOrderStatus**：Draft / Submitted / Approved / Rejected / Cancelled
   - 定义：`backend/internal/constants/change_order_status.go`

### 后端中间件清单（≥ 2 个）

1. **`backend/internal/middleware/auth.go`** — JWT 鉴权
2. **`backend/internal/middleware/rbac.go`** — 角色权限校验（只有 FinanceManager 能审批预算，只有 Accountant 能录入成本）
3. **`backend/internal/middleware/audit_logger.go`** — 审计日志
4. **`backend/internal/middleware/error_handler.go`** — 统一异常
5. **`backend/internal/middleware/rate_limiter.go`** — 接口限流（基于 Redis，每 IP 每分钟 60 次）
6. **`backend/internal/middleware/request_logger.go`** — 请求日志

### 技术要求

- **英文短名（用于 Docker Compose）**：`cost-control-api`
- **后端端口**：19203（本地） / 8080（Docker 内部）
- **后端框架**：Go 1.22 + Gin（github.com/gin-gonic/gin）
- **ORM**：GORM（gorm.io/gorm + gorm.io/driver/postgres）
- **数据库**：PostgreSQL 15
- **缓存**：Redis 7（限流 + 报表缓存），客户端 github.com/redis/go-redis/v9
- **认证**：JWT（github.com/golang-jwt/jwt/v5）+ RBAC；密码哈希 golang.org/x/crypto/bcrypt
- **API 文档**：github.com/swaggo/swag + github.com/swaggo/gin-swagger（挂载于 `/docs`，OpenAPI JSON 位于 `/swagger/doc.json`）
- **代码规范**：严格遵循单一职责原则，每个文件只负责一个功能；常量（错误码、枚举、配置项）统一维护在 `backend/internal/constants/` 目录下；日志统一使用 `log/slog`，日志配置在 `backend/internal/logger/logger.go`；异常信息统一定义在 `backend/internal/constants/error_codes.go`，后端通过 Gin 中间件统一捕获并返回标准 JSON 格式响应（`{code, message, data}`）。

### 文件结构强制清单

**后端（严禁合并职责到单一文件，Go 标准布局）**：
```
backend/
├── go.mod
├── go.sum
├── Dockerfile
├── cmd/
│   └── server/
│       └── main.go
├── database/
│   ├── migrations/        # 建表 SQL / GORM AutoMigrate 迁移
│   └── seeds/             # 种子数据（预置用户、角色）
└── internal/
    ├── config/            # config.go（PostgreSQL、Redis、JWT、Server）
    ├── model/             # project_budget.go, cost_item.go, change_order.go, cost_report.go, user.go, audit_log.go
    ├── repository/        # project_budget_repository.go, cost_item_repository.go, change_order_repository.go, cost_report_repository.go
    ├── service/           # project_budget_service.go, cost_item_service.go, change_order_service.go, cost_report_service.go, analytics_service.go
    ├── handler/           # project_budget_handler.go, cost_item_handler.go, change_order_handler.go, cost_report_handler.go
    ├── router/            # router.go, budgets.go, cost_items.go, change_orders.go, reports.go
    ├── middleware/        # auth.go, rbac.go, audit_logger.go, error_handler.go, rate_limiter.go, request_logger.go
    ├── dto/               # project_budget_dto.go, cost_item_dto.go, change_order_dto.go, cost_report_dto.go
    ├── constants/         # budget_status.go, cost_category.go, change_type.go, change_order_status.go, roles.go, error_codes.go
    ├── util/              # jwt.go, response.go, calculator.go（差异金额自动计算）
    └── logger/            # logger.go（log/slog）
```

后端本地构建与运行命令：
- 依赖安装：`go mod tidy`
- 编译检查：`go build ./...`
- 本地运行：`go run ./cmd/server`

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

## 部署要求

后端 + 数据库 + Redis，Docker Compose 编排。

需要提供以下文件：
- 根目录 `docker-compose.yml`（编排后端 + PostgreSQL + Redis）
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=cost-control-api`
- `backend/Dockerfile`：Go 多阶段构建
- `.env.example`

backend/Dockerfile 必须采用 Go 多阶段构建：

```dockerfile
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: cost-control-api`
- 容器名带 `${COMPOSE_PROJECT_NAME:-cost-control-api}` 前缀
- PostgreSQL + Redis 均使用命名卷持久化
- PostgreSQL `healthcheck`（如 `pg_isready`），后端 `depends_on: { db: { condition: service_healthy } }`
- 后端服务内部端口统一为 `8080`
- 端口映射：
  - 后端：`${BACKEND_PORT:-19203}:8080`
  - PostgreSQL：`${DB_PORT:-5432}:5432`（可选，便于本地调试）
  - Redis：`${REDIS_PORT:-6379}:6379`（可选，便于本地调试）
- 访问 Swagger 文档：http://localhost:19203/docs

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=cost-control-api`
- `DB_NAME` / `DB_USER` / `DB_PASSWORD` / `DB_ROOT_PASSWORD`
- `JWT_SECRET`
- `REDIS_URL`（或 `REDIS_HOST` / `REDIS_PORT`）
- `BACKEND_PORT` / `DB_PORT` / `REDIS_PORT`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：项目介绍、API 功能列表、Docker 快速启动、本地开发方式（`go mod tidy` 和 `go run ./cmd/server`）、Swagger 文档地址（http://localhost:19203/docs）、技术栈表格（后端一栏写 `Go 1.22 + Gin + GORM + PostgreSQL + Redis`）、项目目录结构（代码块展示）、主要 API 列表、环境变量说明、枚举定义位置说明（BudgetStatus / CostCategory / ChangeType / ChangeOrderStatus）、License。

要求：Docker 部署命令放在最前面；后端本地开发命令为 `go mod tidy` 和 `go run ./cmd/server`；构建命令 `go build ./...`；告知用户首次启动前执行 `cp .env.example .env`。

## Git 要求

代码编写完成后：
1. 在项目根目录初始化 Git 仓库（`git init`）
2. 创建 `.gitignore` 文件，至少包含：
   ```
   node_modules/
   dist/
   .env
   .DS_Store
   *.log
   .idea/
   .vscode/
   backend/server
   backend/bin/
   *.test
   *.out
   coverage.out
   data/
   ```
3. 执行 `git add -A && git commit -m "init: 项目初始化"`
